# Diagrams

All diagrams use Mermaid. Render them via VS Code Mermaid plugin or the docs site build.

- `system-architecture.md` — High-level component view.
- `mail-sync-flow.md` — Sequence for syncing external provider.
- `auth-sequence.md` — Magic-link + OAuth sequence.

> Update diagram references when APIs or infrastructure changes.

